<%= form_with(model: ticket_type) do |form| %>
  <% if ticket_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket_type.errors.count, "error") %> prohibited this ticket_type from being saved:</h2>

      <ul>
        <% ticket_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :sla_hours, style: "display: block" %>
    <%= form.number_field :sla_hours %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
